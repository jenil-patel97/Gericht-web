import React from "react";

const SubHeading = ({ title }) => (
  <div className="mb-[1rem]">
    <p className="font-base text-white py-[100px] -mt-2 lg:text-[23px] text-[22px] ml-2 font-semibold cursor-default">
      {title}
    </p>
  </div>
);

export default SubHeading;
